# 桌面画布 Beta 0.1

发布日期：2026-09-20

## 核心能力

- 选择“主任务窗口”和“关注窗口”，按 50:50 至 75:25 比例划分桌面。
- 工作台启用期间持续限制两个窗口的最大范围。
- 窗口最大化、拖动越界或进入 macOS 系统全屏后，会自动回到所属区域。
- 停止工作台后恢复两个窗口启用前的位置和尺寸。
- 支持从菜单栏快速启用、停止和刷新工作台。
- 仅使用辅助功能权限读取窗口名称、位置和尺寸，不读取屏幕内容。

## 系统要求

- macOS 14 或更高版本。
- 首次运行需在“系统设置 → 隐私与安全性 → 辅助功能”中允许桌面画布。
- 安装包同时包含 Apple Silicon 与 Intel 64 位架构。

## 当前限制

- 一个工作台目前只管理两个普通窗口。
- 启用前不能选择已经处于系统全屏的窗口。
- 目标显示器由当前主显示环境决定，暂不支持手动指定显示器。
- 某些应用强制的最小窗口尺寸可能导致实际比例出现少量偏差。
- 本 Beta 使用临时签名且尚未经过 Apple 公证；在其他 Mac 首次启动时，可能需要右键应用并选择“打开”。
