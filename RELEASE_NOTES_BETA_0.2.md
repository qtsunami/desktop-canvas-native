# 桌面画布 Beta 0.2

发布日期：2026-09-20

## 本次新增

- 支持在多显示器环境中手动选择工作台所在显示器。
- 工作台运行期间可以直接修改 50:50 至 75:25 的窗口比例，无需先停止。
- 工作台运行期间可以交换主任务的左右位置，或把整个工作台移动到另一台显示器。
- 新增 50:50、60:40、70:30、75:25 四个常用比例快捷入口。
- 自动记住上次使用的比例、主任务位置和目标显示器。
- 窗口选择菜单显示对应的应用图标，主界面使用新的桌面画布应用图标。
- 菜单栏新增比例、左右位置和目标显示器的快速调整入口。

## 体验与稳定性

- 显示器连接状态变化后会刷新可用显示器；当前目标不可用时自动回退到主显示器。
- 运行中调整布局不会覆盖启用前的窗口快照，“停止并恢复”仍会回到最初位置与尺寸。
- 增加运行中切换比例和左右位置的真实窗口集成验证。
- 版本号更新为 0.2.0（Build 2），打包脚本默认生成 Beta 0.2 安装包。

## 系统要求

- macOS 14 或更高版本。
- 首次运行需在“系统设置 → 隐私与安全性 → 辅助功能”中允许桌面画布。
- 安装包同时包含 Apple Silicon 与 Intel 64 位架构。

## 当前限制

- 一个工作台仍只管理两个普通窗口。
- 启用前不能选择已经处于系统全屏的窗口。
- 两个受管窗口必须位于同一目标显示器，暂不支持跨显示器分别布局。
- 某些应用强制的最小窗口尺寸可能导致实际比例出现少量偏差。
- 本 Beta 使用临时签名且尚未经过 Apple 公证；在其他 Mac 首次启动时，可能需要右键应用并选择“打开”。
